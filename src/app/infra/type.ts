/**
 *
 * @see {@link https://firebase.google.com/docs/auth/admin/errors?hl=ja}
 * @see {@link https://firebase.google.com/docs/reference/node/firebase.auth.Error}
 *
 */
export type FirebaseAuthError = {
	code: string
	message: string
}
